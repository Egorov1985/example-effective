spring:
  application:
    name: effective-react
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/postgresDB
    username: postgres
    password: postgres123
    name: postgresDB
  jpa:
    hibernate:
      ddl-auto: validate
ru:
  egorov:
    dev-url: http://localhost:8080/
springdoc:
  swagger-ui:
    path: "/documentation"
    try-it-out-enabled: true
  api-docs:
    path: "/api/docs"
  packages-to-scan: "ru.egorov.effectiveexample.controller"

logging:
  pattern:
    dateformat: "yyyy-MM-dd HH:mm:ss"
  file:
    name: application.log